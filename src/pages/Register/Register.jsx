const {
  default: RegisterForm,
} = require('components/RegisterForm/RegisterForm');

const Register = () => {
  return (
    <div>
      <RegisterForm />
    </div>
  );
};

export default Register;
